apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: alfa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          # ← Замените на ваш registry
          image: 192.168.0.159:30500/alfa-backend:latest
          imagePullPolicy: Always 
          ports:
            - containerPort: 8080   # Стандартный порт ASP.NET Core 8+
          env:
            # Передаем готовую строку подключения целиком!
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: ConnectionStrings__DefaultConnection
          # ASP.NET health check (если у вас есть HealthChecks endpoint)
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      dnsPolicy: Default
      dnsConfig:
        nameservers:
          - 8.8.8.8
          - 8.8.4.4